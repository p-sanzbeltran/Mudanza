#include <iostream>
/ * Cambiar mayusculas por minusculas * /
int dev_lon (char pal []) {
    int lon = 0;
    int i = 0;
    while (amigo [i]! = '\ 0') {
      lon ++;
      i ++;
    }
    volver (lon);
}
int main () {
  char salir;
  char i, amigo;
  int a;
  std :: cout << "Dime la palabra:";
  std :: cin >> amigo;
  para (i = 0; i <dev_lon (pal); i ++) {
      
  }
  std :: cout << "Dime el codigo ascii" << std :: endl;
  std :: cin >> a;
  i = a;
  std :: cout << "El caracter es:" << i << std :: endl;
  std :: cin >> salir;
}
